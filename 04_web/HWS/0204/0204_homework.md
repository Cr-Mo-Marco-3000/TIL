# 0204_homework



## 1.

```html
<!-- 주어진 목록에서는 다음과 같은 것들이 추가된 semantic tag이다. -->
<!-- header, section, footer, h1  -->
```



## 2.

```html
<label for="username">USERNAME :</label>
<input id="username" type="text" placeholder="아이디를 입력 해 주세요." ><br>

<label for="PWD">PWD :</label>
<input id="PWD" type="password" placeholder="**********" >
<input type="submit" value="로그인">
```



## 3.

```css
<!-- html 최상위 요소의 사이즈를 기준으로 삼는 크기 단위는 rem이다.
html의 기본 폰트 사이즈는 16px이므로, 이걸 기준으로 삼는다. -->
```



## 4.

```css
<!-- 자손 결합자는 자신의 하위 항목들 모두에 있는 태그에 특성을 적용한다.
예시에서의 예를 들자면, div 태그 바로 아래, 또는 더 깊이에 있는 모든 p에 crimson을 적용한다.
자식 결합자는 자신의 바로 하위 항목에만 특성을 적용한다.
예시에서는, div 태그 바로 하위 항목의 p 에만 crimson을 적용한다. -->
```

