nums = [4, 1, 9, 22, 15]

idx = 0

for k in range(len(nums)):
    if nums[idx] > nums[i]:
        min = k

print(idx, nums[idx])

# 선택정렬

# 삽입정렬

# 버블정렬

# 위 3개의 알고리즘은 효율이 좋지 않다.

# 병합정렬
# 정렬할 때 여러 그룹으로 나누어서 진행

# 퀵정렬

"""
"""
